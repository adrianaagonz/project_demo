import pandas as pd


def build_features(df: pd.DataFrame) -> pd.DataFrame:
    """ Keep the data as it so the code can continue"""
    # Just return the dataframe to move to the next step.
    return df 